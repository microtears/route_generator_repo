name: example_library
description: route_generator Flutter example library.
version: 0.0.1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  route_annotation: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
