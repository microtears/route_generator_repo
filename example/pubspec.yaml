name: example
description: route_generator Flutter example project.
version: 1.1.0

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  route_annotation: ^0.2.0
  example_library:
    path: ../example_library

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.1
  route_generator: ^0.1.4

flutter:

  uses-material-design: true
